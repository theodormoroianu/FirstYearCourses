#ifndef TreapSort_
#define TreapSort_

/**
 * "If you don't know how to solve it just use a treap" - general knowledge
 * 
 * Memory: O(n)
 * Time:   O(n * log(n)) expected
 */

void TreapSort(int [], int);

#endif // TreapSort_